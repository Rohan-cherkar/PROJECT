<nav
  class="navbar navbar-expand-md bg-body-light border-bottom sticky-top custom-navbar"
>
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"  
      ><i
        class="fa-solid fa-route"
        style="color: rgb(241, 86, 94); font-size: 2rem"
      ></i
    ></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse nav-btns" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
       
      </div>
      <div class="navbar-nav">  
        <a class="nav-link " href="/listings/New">Add New Destination</a>
        <% if (!currUser ) { %>
          <a class="nav-link log-btn signup" href="/signup">Sign up</a>
          <a class="nav-link log-btn login" href="/login">Login</a>
        <% } %>
          <% if (currUser ) { %>
            <a class="nav-link logout" href="/logout">Log out</a>
          <% } %>
   
      </div>
    </div>
  </div>
</nav>
